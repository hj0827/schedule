<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hj.web.teacher.mapper.TeacherMapper">
    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM teacher WHERE teacher_name = #{teacherName}
    </select>
</mapper>